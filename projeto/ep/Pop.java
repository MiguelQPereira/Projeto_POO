package ep;

/**
 * The Pop interface represents a population.
 */
public interface Pop {

    /**
     * Adds an individual to the population.
     * 
     * @param ind The individual to add to the population.
     */
    void addInd(Individuo ind);
}
